Completely redesign the dashboard with a top navigation bar and 4 pages — no more single long scroll:
TOP NAV BAR (always visible, all pages):
Fixed bar at top: ⚡ Jim Bot | regime badge colored (🔴 BEAR / 🟢 BULL / 🟡 CHOPPY) | Total portfolio value = $1,000 + closed P&L + unrealized P&L combined, shown as $1,034 (+3.4%) | open positions count | fast loop status green dot | last refresh time. Four nav tabs: HOME / MARKET / SIGNALS / PERFORMANCE. Auto-refresh every 15 seconds.
PAGE 1 — HOME (default):
Main section: OPEN POSITIONS table. One single row per asset — never multiple rows for the same asset. Each row shows: symbol + LONG/SHORT badge (green/red), entry price, current price, unrealized P&L in dollars and %, a ‘secured’ green badge showing total partial profits already taken on this position (e.g. ✅ +$1.50), current stop price, regime-adjusted position size %. A subtle green/red left border on each row indicates profit/loss. Click any row to expand a detail panel showing: full trade history for that asset, synthesis score breakdown, Claude’s reasoning, ATR stop calculation. Below the table: one summary line showing Closed P&L + Unrealized P&L + Total Portfolio Value combined.
PAGE 2 — MARKET:
Three equal columns, no scroll: LEFT = Top Movers Today (6 stocks, symbol + % change + up/down arrow, 60s refresh). CENTER = Market Sentiment (regime details, sentiment badge, top 3 headlines with tier badge T1/T2/T3, Trump signal if active, last updated time). RIGHT = Calendar (next macro events with countdown, upcoming earnings with whisper note on hover/click).
PAGE 3 — SIGNALS:
Signal history table showing only the MOST RECENT signal per asset (deduplicated). Columns: time, symbol, signal badge (BUY/SELL/HOLD), confidence %, synthesis final score, regime at time of signal. Click any row to expand: full synthesis score breakdown (Base + Regime + RelStr + DXY + Corr + Geo + News = Final), Claude’s full reasoning text, candlestick pattern detected, ATR stop/target levels. Add a filter bar at top: All / BUY / SELL / HOLD.
PAGE 4 — PERFORMANCE:
Top row: 4 KPI cards — Total Return %, Win Rate %, Profit Factor, Best Asset this week. Below: Daily P&L bar chart (green/red bars). Below: Monthly P&L bar chart. Below: Costs panel (Replit $20 + Claude API calculated from actual calls + Total + Net P&L after costs). All charts use Chart.js.
Remove entirely: the old watchlist cards grid showing every asset as HOLD 20% — replaced by the open positions table on HOME. Remove the repeated signal history rows for same asset — replaced by deduplicated SIGNALS page.
Mobile: on screens under 768px, hide the top movers and calendar columns on MARKET page and show them as expandable sections below sentiment. Keep HOME page exactly the same — it’s already clean enough for mobile.”**