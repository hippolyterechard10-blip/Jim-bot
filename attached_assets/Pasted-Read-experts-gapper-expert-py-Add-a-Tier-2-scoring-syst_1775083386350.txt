Read experts/gapper_expert.py. Add a Tier 2 scoring system that runs after all existing Tier 1 filters pass (gap%, volume, float, price, time window). Do NOT change any existing Tier 1 logic. Make these exact changes:
First, in _enrich_with_float(), add candidate["short_pct"] = info.get("shortPercentOfFloat") or 0.0 alongside the existing float extraction, so short interest is fetched in the same yfinance call with no extra cost.
Then, right after all Tier 1 checks pass and just before capital_to_use is calculated, add this Tier 2 block: initialize tier2_score = 0 and tier2_log = []. Score volume acceleration: if volume_ratio >= 100 → tier2_score += 2; tier2_log.append(f"vol={volume_ratio:.0f}x(+2)"), elif volume_ratio >= 10 → tier2_score += 1; tier2_log.append(f"vol={volume_ratio:.0f}x(+1)"), else → tier2_log.append(f"vol={volume_ratio:.1f}x(+0)"). Score float quality: if float_shares is not None and float_shares < 1_000_000 → tier2_score += 2; tier2_log.append("float<1M(+2)"), elif float_shares is not None and float_shares < 5_000_000 → tier2_score += 1; tier2_log.append("float<5M(+1)"). Score first candle size: if first_candle_low > 0, compute candle_pct = (first_candle_high - first_candle_low) / first_candle_low; if candle_pct > 0.05 → tier2_score += 1; tier2_log.append(f"candle={candle_pct:.1%}(+1)"). Score short interest: short_pct = candidate.get("short_pct", 0) or 0; if short_pct > 0.20 → tier2_score += 1; tier2_log.append(f"short={short_pct:.0%}(+1)"). Then log: logger.info(f"[GAPPER] {symbol} — Tier2={tier2_score} [{', '.join(tier2_log)}]").
Then replace the sizing line with a multiplier approach that respects the existing float-based size_pct: size_multiplier = 1.5 if tier2_score >= 4 else 1.2 if tier2_score >= 2 else 1.0 then capital_to_use = min(available, config.STRATEGY_CAPITAL["gapper"] * min(size_pct * size_multiplier, 0.35)). This way a CYCN (float<1M, vol 8789x, big candle) gets size_pct=0.10 × 1.5 = 15% while a borderline 3x volume stock stays at 10% — without overriding the float-based cap.
Finally add to market_context: "tier2_score": tier2_score, "volume_ratio": round(volume_ratio, 1), "short_pct": round(short_pct, 3), "candle_pct": round(candle_pct if first_candle_low > 0 else 0, 4).
After changes: restart and confirm [GAPPER] Tier2=X [...] appears in logs for every evaluated candidate.​​​​​​​​​​​​​​​​