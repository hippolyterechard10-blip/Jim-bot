Implement a rich trade analysis system across 4 files. Fix 1 — in memory.py: in _init_db() add two ALTER TABLE migrations with try/except: ALTER TABLE trades ADD COLUMN entry_snapshot TEXT and ALTER TABLE trades ADD COLUMN exit_vs_target REAL. In log_trade_open() add entry_snapshot=None parameter and include it in the INSERT as json.dumps(entry_snapshot) if entry_snapshot else None. In log_trade_close() change the SELECT to also fetch take_profit: SELECT entry_at, entry_price, qty, side, take_profit FROM trades WHERE trade_id=?. Then after computing pnl_pct, add: take_profit = row["take_profit"] then exit_vs_target = None then if take_profit and take_profit > 0 and row["entry_price"] > 0: target_gain = (take_profit - row["entry_price"]) / row["entry_price"]; actual_gain = (exit_price - row["entry_price"]) / row["entry_price"]; exit_vs_target = round((actual_gain / target_gain) * 100, 1) if target_gain != 0 else None. Add exit_vs_target=? to the UPDATE statement. Fix 2 — in agent.py add a _build_entry_snapshot(self, symbol, indicators, patterns, session_ctx, synth, decision, current_price) method returning a dict with: rsi, macd_bullish, volume_ratio, atr_pct, bb_pct, momentum_5, above_sma20, change_pct from indicators; patterns list and base_score from patterns; final_score, score_breakdown, regime, support, resistance, risk_reward, stop_pct, target_pct from synth; session from session_ctx; confidence, strategy_used, reasoning from decision; entry_price=current_price. Then in both fast_loop_tick and run_cycle, call entry_snap = self._build_entry_snapshot(...) before log_trade_open() and pass entry_snapshot=entry_snap. Fix 3 — in analyzer.py, update the post-trade analysis prompt to include entry context. Load entry_snapshot from the trade as JSON. Build the prompt using string concatenation (NOT .format() — use explicit variable substitution to avoid crashes if reasoning contains curly braces). Include: entry session, strategy, score breakdown, regime, confidence, RSI, MACD, volume ratio, patterns detected, support/resistance, R:R, the original Claude reasoning, exit reason, and exit_vs_target. Ask Claude to assess entry quality, exit timing, and give one specific actionable improvement. Fix 4 — in notifier.py, update the daily email to show for each real trade (exclude close_reason=‘partial_profit_remainder’): strategy used, score/100, session, regime, confidence, volume ratio, patterns, R:R, exit reason, and exit_vs_target as percentage of objective reached. After all fixes confirm and restart.​​​​​​​​​​​​​​​​