Make these 6 fixes: Fix 1 — in agent.py in init, after self._partial_taken: set = set() add these lines to persist across restarts: if self.memory: /     try: /         _today = __import__("datetime").datetime.now(__import__("datetime").timezone.utc).strftime("%Y-%m-%d") /         _saved_date = self.memory.get_memory("partial_taken_date", "") /         _saved = self.memory.get_memory("partial_taken_today", []) /         self._partial_taken = set(_saved) if _saved_date == _today else set() /     except: pass. Then in _manage_trailing_stops(), after self._partial_taken.add(symbol) add: if self.memory: /     try: /         import datetime as _dt /         self.memory.set_memory("partial_taken_today", list(self._partial_taken), "state") /         self.memory.set_memory("partial_taken_date", _dt.datetime.now(_dt.timezone.utc).strftime("%Y-%m-%d"), "state") /     except: pass. Fix 2 — in strategy.py, replace the entire _atr() function with: def _atr(prices: np.ndarray, period: int = 14) -> float: /     if len(prices) < 2: return 0.0 /     trs = [max(abs(prices[i] - prices[i-1]), abs(prices[i] - prices[i-2]) if i > 1 else 0) for i in range(1, len(prices))] /     return float(np.mean(trs[-period:])). Fix 3 — in config.py, remove the line MAX_LEVERAGE = 3 entirely. Fix 4 — in geometry.py in find_support_resistance(), remove the 5 lines that compute round_levels (the list comprehension starting with magnitude = 10 ** through the closing bracket) since it is computed but never used anywhere in the function. Fix 5 — in synthesis.py, change confidence_min = regime_params["confidence_threshold"] to confidence_min = regime_params["confidence_threshold"] / 100 and update the return dict to "confidence_minimum": confidence_min. Fix 6 — in synthesis.py in the run() method, change changes = self.correlations.refresh_prices([symbol]) to _peer_symbols = list(set([s for pair in self.correlations.CORRELATION_MATRIX.keys() for s in pair] + [symbol])) then changes = self.correlations.refresh_prices(_peer_symbols). After all fixes confirm each change and restart.​​​​​​​​​​​​​​​​