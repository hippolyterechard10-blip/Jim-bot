Make these 10 fixes. Fix 1 — in scanner.py, change tradeable = [a for a in assets if a.tradable and a.fractionable] to tradeable = [a for a in assets if a.tradable]. Fix 2 — in scanner.py, in get_top_movers(), change if age < 900 and self._movers_cache["symbols"]: to if age < 300 and self._movers_cache["symbols"]:. Fix 3 — in agent.py in fast_loop_tick(), change for m in cached_movers[:5]: to for m in cached_movers[:10]:. Fix 4 — in agent.py in run_cycle(), find the block if size_mult != 1.0: that multiplies qty and pct by size_mult and DELETE the entire block — risk.py already applies the regime multiplier via get_position_size_by_score(), doing it again here creates 0.36x positions. Fix 5 — in agent.py in run_cycle(), change _side = "short" if base_score < 45 else "long" to _side = "short" if base_score < score_short_max else "long". Fix 6 — in agent.py in analyze_market(), change combined_context = f"{market_context}\n\n{regime_context}" if market_context else regime_context to combined_context = market_context if market_context else regime_context — regime is already in market_context from synthesis. Fix 7 — in strategy.py in compute_opportunity_score(), find score += min(8, pattern_sc // 5) inside the if pattern_act == "buy" block and replace it with if "GAPPER" in patterns.get("patterns", []): score += 35 then else: score += min(8, pattern_sc // 5). Fix 8 — in dashboard.py in api_stops(), replace getattr(_agent, "_trailing_high", {}) with getattr(_agent, "_high_water", {}), replace getattr(_agent, "_score_trail_pct", {}) with getattr(_agent, "_trail_pcts", {}), replace getattr(_agent, "_trailing_low", {}) with getattr(_agent, "_low_water", {}), and remove the s_trail variable entirely. Fix 9 — in agent.py in the SHORT SELLING PASS in run_cycle(), replace geo_sl = data.get("synthesis", {}).get("stop_loss") and sl = geo_sl if geo_sl else self.risk.calculate_stop_loss(current_price, "sell") with just sl = self.risk.calculate_stop_loss(current_price, "sell"). Fix 10 — in agent.py in run_cycle(), after the beta adjustment block that sets pct = beta_pct / 100, add: if pct < 0.05: logger.info(f"  ↳ Position floor: {symbol} {pct*100:.1f}% below 5% minimum after adjustments — skipping"); continue. After all 10 fixes, confirm each one was applied and show the exact line changed, then restart the agent.